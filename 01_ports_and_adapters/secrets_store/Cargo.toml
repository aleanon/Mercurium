[package]
name = "secrets_store"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[features]
# Exposes in-memory test doubles (InMemorySecretsStore) for headless verification.
testing = []

[dependencies]
deps = { path = "../../0_deps" }
types = { path = "../../1_types" }
