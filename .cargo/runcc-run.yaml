commands:
  watch: cargo watch -w iced_ui -x "rustc --package iced_ui --crate-type dylib --profile reload --features reload"
  run: cargo run --profile reload --features reload --target-dir target-bin
